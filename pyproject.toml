[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "audio-violence-detection-tinyml"
version = "0.1.0"
description = "Audio violence detection utilities and notebooks"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["scripts"]
